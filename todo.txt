Next up:
	Hide all button ==done
	search for items == DONE just fix it up so that it comes up in the middle of the page as an element and not an popup
	move items == there's no need for a button, just tell them they're wrong and move it, can be undon using undo button
	undo button
	voice
	tutorial
	settings?
	multilevel storage
	run tests
	add date logs
	